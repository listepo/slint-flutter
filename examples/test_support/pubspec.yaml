name: slint_example_test_support
resolution: workspace
description: Shared helpers for Flutter example widget tests.
version: 1.18.0
publish_to: none

environment:
  sdk: ^3.5.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  slint:
    path: ../../slint
  slint_flutter:
    path: ../../slint_flutter
